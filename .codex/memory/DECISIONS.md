# Decisions

## Confirmed

- Keep the requested GitHub repository name with underscores, while using the validated hyphenated Codex skill name.
- Retain and publicly upload the source PPTX because the user explicitly authorized it.
- Treat source PPT slides and source-derived lockup crops as provenance references, not output slide assets.
- Use `assets/hohai-lockup-authorized.jpg` once on every slide as the primary combined emblem and bilingual school-name lockup.
- Allow `assets/hohai-emblem-authorized.png` only as an optional standalone emblem; its transparent margins may be non-destructively cropped to the declared content box.
- Never redraw, generate, recolor, trace, sharpen, background-remove, or reconstruct either user-authorized identity asset.
- Exclude every 110-anniversary text or pixel from generated decks.

